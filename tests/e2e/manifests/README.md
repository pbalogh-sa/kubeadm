## Modified tools for verifying kubeadm required images and manifest lists

Listing stable releases and tag images for mirroring to a private registry.

Example:
```shell
go run verify_manifest_lists.go ghcr.io/test-org
```
